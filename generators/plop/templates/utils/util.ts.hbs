import type {{pascalCase name}}Props from './{{camelCase name}}.types';

const {{camelCase name}} = ({ value = 'Default value' }: {{pascalCase name}}Props): string => {
  return value;
};

export default {{camelCase name}};
