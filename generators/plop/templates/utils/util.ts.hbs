import {{pascalCase name}}Props from './{{camelCase name}}.types';

const {{camelCase name}} = ({ value = 'Default value' }: {{pascalCase name}}Props) => {
  return value;
};

export default {{camelCase name}};
